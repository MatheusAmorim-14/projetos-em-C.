//  Soma de dois valores inteiros
#include <stdio.h>

int main(){

int A, B, X; // introdução dos termos, A e B, além do X que é o resutaldo 
scanf("%d\n %d", &A, &B); //Pedi pra digitar o primeiro valro A e apertar enter para ir para próxima linha
X = A + B;// Defini que o valor de X vai ser a soma entre A e B
printf("X = %d", X);

return 0;
}