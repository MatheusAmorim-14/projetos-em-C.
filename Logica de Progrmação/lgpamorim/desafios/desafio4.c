#include <stdio.h>

int main(){

//dclaerei as variáveis A,B e SOMA( esse vai armazenar o resultado da soma (que vai poder ser subtração tmb))
int A, B, SOMA;
  
scanf("%d %d", &A, &B);
 
   SOMA = A + B;

printf(" SOMA = %d\n", SOMA);
return 0;
}