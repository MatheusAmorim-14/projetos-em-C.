#include <stdio.h>
// se eu já fiz 3 desafios com consulta esse aqui vai ter que ser na marra!
int main(){

// declara os valores que vão ser usados aqui e bla bla bla 
int A, B, PROD;

// scanf = leia isso aqui softwer, %d pra aplicar um valor de numero decimal &A pra indicar que o valor vai ser nele 
scanf("%d %d", &A, &B);

// Essa é a operação que eu quero múltiplicação * ent PROD = A*B simples né 
PROD = A * B;

// printf serve pra imprimir/ mostrar a mensagem, nesse caso o o resultado PROD
printf("PROD =%d\n", PROD);

// return 0 pra indicar que rodou tudo certo.
return 0;

}