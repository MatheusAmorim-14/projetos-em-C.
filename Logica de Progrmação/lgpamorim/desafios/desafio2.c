#include <stdio.h>

int main(){
int A,B,C, X; // criei quatro espaços na memória do computador para guardar números inteiros.

scanf("%d %d %d", &A, &B, &C); // escolhe qualquer valor para A, B e C, sendo eles reais. 

X = A + B + C; // X= a soma de A+B+C. (Teoriacamente vai rodar liso) E RODOU FML VAMOOS

printf("X = %d\n",X);

return 0; 

}

